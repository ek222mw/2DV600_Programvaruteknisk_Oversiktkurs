package Fraction;

public class FractionMain {
	
	public static void main(String[] args)
	{
		Fraction frac = new Fraction(5,10);
		
		int numerator = frac.getNumerator();
		int denominator = frac.getDenominator();
		
		double add = frac.add(numerator, denominator);
	}

}
